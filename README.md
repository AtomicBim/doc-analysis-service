# Сервис анализа проектной документации

**Версия:** 5.0.0 (Hybrid Architecture)

## Описание

Микросервисная система для автоматического анализа соответствия проектной документации требованиям технического задания (ТЗ) и технических условий (ТУ).

### 🚀 Гибридная архитектура v5.0

**Уникальный подход, сочетающий лучшее из двух миров:**

1. **ТЗ/ТУ** → Ручной парсинг и контролируемая сегментация требований
2. **Чертежи** → OpenAI Assistants API с File Search для интеллектуального анализа

**Преимущества:**
- ✅ **Высокое качество анализа** - Vision модель "видит" чертежи, графику, размеры
- ✅ **Автоматические цитаты** - точные ссылки на листы чертежей
- ✅ **Нет превышения лимитов** - оптимизированное использование токенов
- ✅ **Масштабируемость** - работает с PDF до 512MB

## Архитектура

Система построена на микросервисной архитектуре и состоит из двух независимых сервисов, управляемых через `docker-compose`.

```
┌────────────────────────────────────────────────────────────────────────┐
│                        Корпоративная сеть                              │
│                                                                        │
│  ┌────────────────────┐                                               │
│  │   Пользователь     │                                               │
│  └─────────┬──────────┘                                               │
│            │ HTTP                                                     │
│            ▼                                                           │
│  ┌────────────────────┐          HTTP           ┌──────────────────┐   │
│  │   Gradio UI        │ ─────────────────────► │   API Service    │   │
│  │  (ui-service)      │                        │ (api-service)    │   │
│  │  Port: 7861        │ ◄───────────────────── │   Port: 8002     │   │
│  └────────────────────┘       JSON              └─────────┬────────┘   │
│                                                           │            │
└───────────────────────────────────────────────────────────┼────────────┘
                                                            │
                                              SOCKS5 Proxy  │ VPN
                                              (настраивается) │
                                                            ▼
                                                ┌───────────────────────┐
                                                │      OpenRouter       │
                                                └───────────────────────┘
```

### Компоненты

1.  **`ui-service` (Gradio UI)**
    *   **Назначение:** Пользовательский веб-интерфейс для загрузки документов и просмотра результатов анализа.
    *   **Технологии:** Gradio, Requests.
    *   **Сеть:** Работает в основной сети, доступен по порту `7861`. Не использует прокси.
    *   **Взаимодействие:** Отправляет HTTP-запросы на `api-service`.

2.  **`api-service` (FastAPI) - HYBRID MODE**
    *   **Назначение:** Гибридный анализ документации:
        - Парсинг ТЗ/ТУ и сегментация требований (ручной контроль)
        - Загрузка чертежей в OpenAI Assistants API Vector Store
        - Интеллектуальный анализ каждого требования через File Search
    *   **Технологии:** FastAPI, OpenAI Assistants API, File Search, Vision (gpt-4o)
    *   **Сеть:** Прямое подключение к OpenAI API (прокси не требуется)

## Структура проекта

```
doc-analysis-service/
├── api-service/                  # ✅ Сервис API (FastAPI)
│   ├── analysis_api.py           #    - Логика API
│   ├── requirements.txt          #    - Зависимости Python
│   ├── Dockerfile                #    - Docker-конфигурация
│   └── .env.example              #    - Пример файла с переменными окружения
│
├── ui-service/                   # ✅ Сервис UI (Gradio)
│   ├── gradio_ui.py              #    - Логика интерфейса
│   ├── requirements.txt          #    - Зависимости Python
│   └── Dockerfile                #    - Docker-конфигурация
│
├── docker-compose.yml            # ✅ Оркестратор для запуска всех сервисов
├── README.md                     # 📖 Этот файл
├── .gitignore
```

## Быстрый старт

### Шаг 1: Настройка переменных окружения

1.  Скопируйте файл с примером переменных окружения для API-сервиса:
    ```bash
    cp api-service/.env.example api-service/.env
    ```

2.  Откройте `api-service/.env` и укажите ваш API-ключ для OpenAI:

    ```env
    # ========================================
    # OPENAI API (обязательно)
    # ========================================
    # Получить ключ: https://platform.openai.com/api-keys
    OPENAI_API_KEY=sk-proj-ВАШ_КЛЮЧ_OPENAI

    # Модель для анализа (рекомендуется gpt-4o для vision)
    OPENAI_MODEL=gpt-4o

    # Temperature (0.0 - детерминированный, 1.0 - креативный)
    TEMPERATURE=0.1
    ```

    **Важно:** Для гибридной архитектуры используется **OpenAI API**, а не OpenRouter.
    - Требуется модель с поддержкой vision (gpt-4o или gpt-4o-mini)
    - Assistants API включен автоматически

### Шаг 2: Настройка прокси (опционально)

**Для гибридной архитектуры прокси НЕ требуется**, т.к. используется прямое подключение к OpenAI API.

Если у вас корпоративный firewall, убедитесь что открыт доступ к:
- `api.openai.com` (порт 443)

### Шаг 3: Сборка и запуск

Выполните команду в корневой директории проекта:

```bash
docker-compose up -d --build
```
Эта команда соберёт образы для UI и API сервисов и запустит их в фоновом режиме.

### Шаг 4: Доступ к сервисам

-   **Веб-интерфейс (Gradio):** [http://localhost:7861](http://localhost:7861)
-   **Документация API (Swagger):** [http://localhost:8002/docs](http://localhost:8002/docs)

## Использование

1.  Откройте веб-интерфейс в браузере.
2.  Загрузите файлы: ТЗ, проектную документацию и (опционально) ТУ.
3.  Выберите параметры анализа: стадию документации и тип анализируемых требований.
4.  Нажмите кнопку **"Выполнить анализ"**.
5.  Результаты анализа появятся в интерфейсе в формате Markdown.

## Тестирование и отладка

### Проверка статуса контейнеров

```bash
# Показать запущенные контейнеры
docker-compose ps

# Просмотр логов API-сервиса в реальном времени
docker-compose logs -f doc-analysis-api

# Просмотр логов UI-сервиса
docker-compose logs -f doc-analysis-ui
```

### Тестирование API через cURL

Этот способ позволяет проверить работу API-сервиса напрямую, без UI.

1.  Создайте тестовые файлы:
    ```bash
    echo "Это тестовое ТЗ." > test_tz.txt
    echo "Это тестовая документация." > test_doc.txt
    ```

2.  Отправьте запрос на анализ:
    ```bash
    curl -X POST http://localhost:8002/analyze \
      -F "stage=ФЭ" \
      -F "req_type=ТЗ" \
      -F "tz_document=@test_tz.txt" \
      -F "doc_document=@test_doc.txt"
    ```

### Проверка доступа к OpenRouter API из контейнера

Эта команда позволяет убедиться, что прокси настроен верно и `OPENROUTER_API_KEY` валиден.

```bash
docker-compose exec doc-analysis-api python -c "
import os
from openai import OpenAI

client = OpenAI(
  base_url='https://openrouter.ai/api/v1',
  api_key=os.getenv('OPENROUTER_API_KEY'),
)

try:
    completion = client.chat.completions.create(
        model='mistralai/mistral-7b-instruct',
        messages=[{'role': 'user', 'content': 'Привет!'}],
        temperature=0.0
    )
    print('Успешно! Ответ от OpenRouter:')
    print(completion.choices[0].message.content)
except Exception as e:
    print(f'Ошибка подключения к OpenRouter API: {e}')
"
```

## API Endpoints

### `GET /`

-   **Описание:** Health check для API-сервиса.
-   **Ответ:** JSON с информацией о статусе и используемой модели.
    ```json
    {
      "status": "ok",
      "service": "Document Analysis API",
      "model": "anthropic/claude-3-haiku"
    }
    ```

### `POST /analyze`

-   **Описание:** Основной эндпоинт для анализа документов. Принимает данные в формате `multipart/form-data`.
-   **Параметры формы:**
    -   `stage` (string): Стадия проекта (например, "ФЭ").
    -   `req_type` (string): Тип требований (например, "ТЗ").
    -   `tz_document` (file): Файл технического задания.
    -   `doc_document` (file): Файл проектной документации.
    -   `tu_document` (file, optional): Файл технических условий.
-   **Пример ответа (JSON):**
    ```json
    {
      "stage": "ФЭ",
      "req_type": "ТЗ",
      "requirements": [
        {
          "number": 1,
          "requirement": "Текст требования из ТЗ",
          "status": "Исполнено",
          "confidence": 95,
          "solution_description": "Краткое описание, как требование реализовано в документации.",
          "reference": "Ссылка на раздел/пункт документации, стр. X",
          "discrepancies": "Выявленные несоответствия (если есть).",
          "recommendations": "Рекомендации по устранению несоответствий (если есть)."
        }
      ],
      "summary": "Общая сводка по результатам анализа."
    }
    ```

## Лицензия

Проект создан для внутреннего использования.

## 🎯 Как работает гибридный анализ (v5.0)

Этот проект использует **инновационный гибридный подход**, сочетающий ручную обработку с передовыми возможностями Assistants API:

### 📋 Этап 1: Парсинг ТЗ/ТУ (ручной контроль)
**Почему вручную:** Точная сегментация требований критична для качества анализа

1. **Извлечение текста из ТЗ/ТУ** - PyMuPDF парсит PDF
2. **Сегментация требований** - gpt-4o-mini разбивает на отдельные требования с номерами и разделами
3. **Результат:** Структурированный список из 20-50 требований

**Преимущества:**
- ✅ Полный контроль над извлечением требований
- ✅ Дешево (используется gpt-4o-mini)
- ✅ Быстро (< 1 минуты)

---

### 📤 Этап 2: Загрузка чертежей в Vector Store

**Почему Assistants API:** Нативная поддержка PDF до 512MB + автоматический парсинг

1. **Создание Vector Store** - OpenAI создает векторное хранилище
2. **Загрузка PDF чертежей** - Файл загружается целиком
3. **Автоматический парсинг** - OpenAI парсит текст, таблицы, графику
4. **Результат:** Vector Store готов к File Search

**Преимущества:**
- ✅ Не нужно делать эмбеддинги вручную
- ✅ Поддержка очень больших файлов
- ✅ OpenAI оптимизирует хранение

---

### 🤖 Этап 3: Создание Assistant

**Настройка:** Assistant получает промпт для стадии (ГК/ФЭ/ЭП) + инструкции по анализу

```python
tools=[{"type": "file_search"}]  # Включаем File Search
model="gpt-4o"  # Vision модель для чертежей
```

---

### 🔍 Этап 4: Анализ каждого требования

**Для каждого требования (параллельно):**

1. **Создание Thread** - новый диалог с Assistant
2. **Отправка требования** - текст требования + контекст
3. **File Search** - Assistant автоматически ищет релевантные части чертежей
4. **Vision Analysis** - gpt-4o "видит" чертежи, размеры, схемы
5. **Получение ответа** - JSON с анализом + **автоматические citations**

**Пример citation:**
```json
{
  "reference": "Лист АР-03, узел №2",
  "quote": "Отметка заложения фундамента -2.50м"
}
```

**Преимущества:**
- ✅ Assistant сам находит нужные листы (не нужно 25 страниц в контекст)
- ✅ Vision видит графику, размеры, обозначения
- ✅ Точные ссылки на источники
- ✅ Нет превышения лимитов токенов

---

### 📊 Этап 5: Сводка и возврат результата

1. **Генерация статистики** - подсчет статусов, confidence
2. **Cleanup** - удаление временных Assistant и Vector Store
3. **Возврат результата** - JSON с полным анализом

---

### 🆚 Сравнение: Старый vs Гибридный подход

| Аспект | Старый (v2.x) | Гибридный (v5.0) |
|--------|---------------|------------------|
| **Парсинг чертежей** | Ручной (PyMuPDF + вручную эмбеддинги) | **Assistants API (автоматически)** |
| **Поиск релевантных страниц** | Numpy косинусное сходство | **File Search (OpenAI)** |
| **Понимание графики** | Только если есть текст | **Vision (gpt-4o видит чертежи)** |
| **Ссылки на источники** | Ручной парсинг | **Автоматические citations** |
| **Лимит токенов** | ❌ Часто превышение (50K+) | ✅ **Оптимизировано (< 15K)** |
| **Размер файлов** | До 40MB | **До 512MB** |
| **Качество анализа** | ⚠️ Среднее | ✅ **Высокое** |

---

### 💡 Почему этот подход лучше

**Ключевое преимущество:** Сочетание контроля (ТЗ/ТУ) и автоматизации (чертежи)

1. **ТЗ/ТУ требуют точности** → ручная сегментация обеспечивает правильную структуру
2. **Чертежи сложны** → Assistants API с vision лучше справляется с графикой
3. **Масштабируемость** → нет ручной обработки тысяч страниц
4. **Экономия** → меньше токенов, меньше стоимость

## Основные функции
- **Мультимодальный анализ:** Обработка текста и изображений чертежей.
- **Автоматическая сегментация ТЗ/ТУ:** Разбор на требования.
- **Ретривал с эмбеддингами:** Поиск релевантных частей документации.
- **Батчинг и параллелизм:** Эффективный анализ больших наборов требований.
- **Настраиваемые промпты:** По стадиям проекта (ГК, ФЭ, ЭП).
- **UI на Gradio:** Простой веб-интерфейс для загрузки и просмотра результатов.
- **Docker-деплой:** Легкий запуск с прокси для API.

Этот подход позволяет заменить ручную проверку на автоматизированную, снижая ошибки и время на экспертизу строительных проектов.