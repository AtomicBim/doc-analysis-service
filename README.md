# 📋 Сервис анализа проектной документации

Интеллектуальная система для автоматического анализа строительной проектной документации с использованием OpenAI GPT-5-mini (Vision API).

## 🎯 Возможности

- **Двухэтапный процесс**: Извлечение требований из ТЗ → Анализ проектной документации
- **Умное извлечение**: Автоматическая сегментация требований из ТЗ (PDF/DOCX)
- **Визуальный анализ**: Анализ чертежей через OpenAI Vision API с высоким разрешением
- **4-стадийный анализ**:
  - Stage 1: Извлечение метаданных страниц (штампы, заголовки)
  - Stage 2: Оценка релевантности страниц для каждого требования
  - Stage 3: Детальный анализ релевантных страниц в высоком качестве
  - Stage 4: Поиск противоречий в документации (опционально)
- **Интерактивный UI**: React-приложение с редактированием требований и навигацией по PDF
- **Навигация по листам**: Автоматическое распознавание номеров листов для точной навигации

## 🏗️ Архитектура

```
┌─────────────────────────────────────────┐
│   Frontend (React + TypeScript)         │
│   - Загрузка файлов                     │
│   - Редактирование требований           │
│   - Просмотр результатов + PDF          │
│   Port: 7862                            │
└────────────────┬────────────────────────┘
                 │ HTTP API
┌────────────────▼────────────────────────┐
│   Backend (FastAPI + Python)            │
│   - OpenAI Vision API Integration       │
│   - PDF Processing (PyMuPDF)            │
│   - Multi-stage analysis pipeline       │
│   Port: 8002                            │
└─────────────────────────────────────────┘
```

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- OpenAI API ключ ([получить здесь](https://platform.openai.com/api-keys))
- (Опционально) SOCKS5/HTTP прокси, если OpenAI API недоступен напрямую

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd doc-analysis-service
```

### 2. Настройка API ключа

Создайте файл `api-service/.env`:

```bash
cd api-service
cp .env.example .env
nano .env  # или используйте любой текстовый редактор
```

Установите ваш OpenAI API ключ:

```env
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

### 3. (Опционально) Настройка прокси

Если требуется прокси для доступа к OpenAI API, создайте файл `.env` в корне проекта:

```bash
cd ..  # вернуться в корень проекта
nano .env
```

Добавьте настройки прокси:

```env
# Для SOCKS5 прокси
HTTPS_PROXY=socks5://127.0.0.1:10808
HTTP_PROXY=socks5://127.0.0.1:10808

# Или для HTTP прокси
# HTTPS_PROXY=http://proxy-server:port
# HTTP_PROXY=http://proxy-server:port
```

Раскомментируйте строки в `docker-compose.yml`:

```yaml
environment:
  # Раскомментируйте эти строки:
  - HTTPS_PROXY=${HTTPS_PROXY:-}
  - HTTP_PROXY=${HTTP_PROXY:-}
```

### 4. Запуск сервиса

```bash
docker-compose up --build
```

Первый запуск займет 3-5 минут для сборки образов.

### 5. Открыть приложение

Откройте браузер и перейдите на:

```
http://localhost:7862
```

## 📖 Использование

### Шаг 1: Извлечение требований из ТЗ

1. Нажмите "Выбрать файл" под "Техническое задание (ТЗ)"
2. Загрузите PDF или DOCX файл с техническим заданием
3. Нажмите "📋 Извлечь требования из ТЗ"
4. Дождитесь обработки (1-3 минуты)
5. Проверьте и отредактируйте извлеченные требования
6. Снимите галочки с нерелевантных требований
7. Нажмите "✓ Подтвердить и продолжить"

### Шаг 2: Анализ проектной документации

1. Выберите стадию проекта (ГК, ФЭ, ЭП)
2. Загрузите PDF файл с проектной документацией
3. Нажмите "🔍 Выполнить анализ проекта"
4. Дождитесь завершения анализа (5-15 минут в зависимости от размера)
5. Просмотрите результаты в левой панели
6. Нажимайте на требования для навигации по соответствующим листам

### Возможности UI

- **Редактирование требований**: Измените текст извлеченных требований перед анализом
- **Выборочный анализ**: Анализируйте только выбранные требования
- **Навигация по PDF**: Клик по требованию автоматически открывает нужный лист
- **Подсветка текста**: Автоматическая подсветка релевантного текста на листе
- **Изменяемые панели**: Перетаскивайте разделители для настройки размеров панелей
- **Сохранение прогресса**: Результаты сохраняются в localStorage браузера

## ⚙️ Конфигурация

### Backend (api-service/config.py)

Ключевые параметры для настройки качества и производительности:

```python
# Stage 2: Оценка релевантности
STAGE2_MAX_PAGES = 100  # Макс. страниц для анализа
STAGE2_DETAIL = "high"  # Качество Vision API: "low" / "high"

# Stage 3: Детальный анализ
STAGE3_BATCH_SIZE = 10  # Требований в одном запросе
STAGE3_MAX_COMPLETION_TOKENS = 8000  # Макс. токенов ответа

# Stage 4: Противоречия
STAGE4_ENABLED = True  # Включить/выключить поиск противоречий

# OpenAI
OPENAI_MODEL = "gpt-5-mini"  # Модель для анализа
```

### Frontend (ui-react-service/package.json)

```json
{
  "proxy": "http://localhost:8002"  # Для локальной разработки
}
```

## 🔧 Разработка

### Запуск без Docker

#### Backend

```bash
cd api-service
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python analysis_api_hybrid.py
```

#### Frontend

```bash
cd ui-react-service
npm install
npm start
```

### Структура файлов

```
doc-analysis-service/
├── api-service/
│   ├── analysis_api_hybrid.py     # Главный API сервис
│   ├── pdf_processor.py           # Обработка PDF
│   ├── config.py                  # Конфигурация
│   ├── requirements.txt           # Python зависимости
│   ├── Dockerfile                 # Образ для API
│   └── .env                       # API ключи (НЕ коммитить!)
├── ui-react-service/
│   ├── src/
│   │   ├── components/            # React компоненты
│   │   ├── hooks/                 # Custom hooks
│   │   ├── utils/                 # Утилиты
│   │   ├── App.tsx                # Главный компонент
│   │   └── types.ts               # TypeScript типы
│   ├── Dockerfile                 # Multi-stage build
│   ├── nginx.conf.template        # Nginx конфигурация
│   └── package.json               # NPM зависимости
├── prompts/                       # AI промпты для каждой стадии
├── docker-compose.yml             # Оркестрация сервисов
└── README.md                      # Эта документация
```

## 🐛 Troubleshooting

### Проблема: "OPENAI_API_KEY не установлен"

**Решение**: Убедитесь что файл `api-service/.env` существует и содержит корректный API ключ.

### Проблема: "Error connecting to OpenAI API"

**Решение**:
1. Проверьте интернет-соединение
2. Если OpenAI API недоступен в вашем регионе, настройте прокси (см. раздел "Настройка прокси")
3. Убедитесь что прокси работает: `curl -x socks5://127.0.0.1:10808 https://api.openai.com`

### Проблема: "Rate limit exceeded"

**Решение**: OpenAI имеет лимиты на количество запросов. Уменьшите параметры в `config.py`:
- `STAGE3_BATCH_SIZE = 5` (вместо 10)
- `STAGE2_MAX_PAGES = 50` (вместо 100)
- `STAGE4_ENABLED = False` (отключить Stage 4)

### Проблема: "PDF viewer не загружается"

**Решение**: Очистите кэш браузера и перезагрузите страницу (Ctrl+Shift+R).

### Проблема: "Container fails to start"

**Решение**:
```bash
# Проверьте логи
docker-compose logs -f

# Пересоберите образы
docker-compose down
docker-compose up --build
```

## 📊 Производительность

### Типичное время обработки

- **Извлечение требований** (ТЗ на 20 страниц): 1-3 минуты
- **Stage 1** (Метаданные 100 страниц): 2-5 минут
- **Stage 2** (Релевантность 30 страниц × 50 требований): 5-10 минут
- **Stage 3** (Детальный анализ 50 требований): 10-30 минут
- **Stage 4** (Противоречия, опционально): 2-5 минут

**Общее время**: 20-50 минут для проекта с 100 страницами документации и 50 требованиями.

### Оптимизация

Для ускорения обработки:
1. Уменьшите `STAGE2_MAX_PAGES` для анализа меньшего количества страниц
2. Увеличьте `STAGE3_BATCH_SIZE` для параллельной обработки (осторожно с rate limits)
3. Отключите `STAGE4_ENABLED` если противоречия не нужны
4. Используйте текстовый префильтр (включен по умолчанию)

## 🔒 Безопасность

- **API ключи**: Храните `.env` файлы в безопасности, НИКОГДА не коммитьте их в Git
- **Read-only volume**: `.env` монтируется в контейнер в режиме read-only
- **Network isolation**: Сервисы изолированы в Docker сети
- **CORS**: Backend настроен на прием запросов только от известных источников

## 📝 Лицензия

Proprietary - все права защищены.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте раздел "Troubleshooting"
2. Просмотрите логи: `docker-compose logs -f`
3. Создайте issue в репозитории с описанием проблемы и логами
